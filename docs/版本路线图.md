# 版本路线图：v5.0「自用投研助手」

> 状态：已按本源确认重写，迭代顺序已排定（2026-08-21）；同日按 v5 设计第三轮 review 修订口径（动作枚举对齐代码 / 挂点位置与映射修正 / 重放滚动窗口 / 日志全量落盘）；**2026-08-22 I7.1–I7.5 五迭代全部完成归档**（提交 cd328b2→c09c27e），增列 I8.1（口径校准与统计保真）；**2026-08-30 I8.6b/I8.6c 完成归档**（提交 112b8d2），评估与响应闭环看板全链路收口；2026-09-01 排定 I9 迭代（选股层与滚动评估，详见 `docs/迭代_i9_选股层/选股层与滚动评估设计.md`）；**2026-09-02 全面更新：增「当前阶段」状态头，补录模拟账户 V6.3–V8 及配套迭代完成记录，重写剩余展望**。
> 本源定位（用户确认）：**自己先用 · 核心价值=实用 · 接受"未经实证"但绝不自欺**
> 项目定位（2026-09-02 确认）：以「绝不自欺」为唯一纪律的**个人投研证据系统**——不预测行情，只回答「策略在什么环境下、以什么代价、有没有优势」；每个结论必须有可复现的证据链与适用口径。模拟账户是策略的实时采样器（回测的实时版，测可执行性）而非交易工具；若证据不支持现有策略假设，按 `docs/策略迭代-第一性原则-v1.md` §5 诚实降级为技术分析看板，而非继续堆规则。

## 当前阶段（2026-09-02）：证据积累期

功能骨架已齐：信号引擎 + 策略门（第一性原则 v1）、评估与响应闭环（I8）、选股层与滚动评估（I9）、模拟账户 v6→v8。**当前主线不是加功能，而是让已有闭环产出可判读的证据**：

1. 月度滚动评估连续积累样本（看板评估页签「历史趋势」）；
2. **口径收敛**——把最终动作（含后处理/策略门）纳入重放并标记 policy 版本（见剩余展望 #1）；
3. 观察策略门拦截信号的后续收益，按第一性原则 §5 的标准验证（不自我说服）。

参数与阈值调整仍走预承诺规则（review T1–T6），75/60 冻结不动。

## 一、版本目标（一句话）

把工具变成"**自己看得懂、信得过、用得顺**"的投研辅助：用轻量历史回测防止自我欺骗，用真实信号日志积累诚实样本，用实用打磨提升日常体验。

## 二、背景与依据

- 五模块策略 + 缠论日/分时已正确性修复完毕（41 个回归测试全绿）；
- 审计报告（docs/策略审核报告.md）：策略未经样本外验证，盈利能力未知；
- 本源对齐（用户确认）：自己用、要实用、接受"未证实" —— 本版**不追求研究级完备**。

## 三、范围内 / 范围外

**范围内**
- Git 基线与统一测试入口（轻量，防手滑/可回滚）；
- **信号日志**：真实信号发生时**全量落盘**（动作枚举照代码：强烈买入/买入/谨慎买入/观望；含 breakout 多头止损与空头平仓，按修订枚举归类），去重窗口内的重复信号标 `deduped`、过滤在展示/统计层做，事后补记 5/10/20/60 交易日走势；
- **核心池管理**：可视化选择/维护、持久化、随时可换（回测与信号日志共用）；
- **历史信号统计**：核心池 × 2–3 年日线，每个买入侧信号（强烈买入/买入）之后的 5/10/20/60 日表现（胜率 / 平均收益 / 期望）+ 可选单信号独立模拟；
- 实用打磨：启动/加载/刷新、核心列表管理、导出/复盘。

**范围外（明确不做）**
- 组合级持仓/权益模拟、资金管理、仓位模型（评估的是信号质量，不是自动交易收益）；
- 大样本分层 / 消融矩阵 / bootstrap CI / 滚动窗口；
- 实盘对接、对外分发；
- 外部历史分钟数据（分钟缠论不做历史回测，由信号日志覆盖真实分钟信号）；
- 任何"盈利能力"宣称。

## 四、迭代划分（顺序已排定，每个迭代 = 一个 Comet change）

### I7.1 `git-baseline-and-test-runner` — 工程基线
Git 初始化、`.gitignore`、首次提交标记 `v4-baseline`；`run_all_tests.py` 一条命令跑全部测试。
**验收**：一条命令全量回归通过；此后每次改动有 diff 可查、可回滚。依赖：无。规模：小。

### I7.2 `signal-journal` — 信号日志（成本最低见效最快）
信号发生时落一条记录（挂点在 app 后处理**之后**，记录最终 action；signal_type 枚举：buy / strong_buy / cautious_buy / breakout_exit / short_cover / chanlun_buy1·2 / chanlun_sell1·2，映射见设计稿 §5.4）；**全量落盘不丢弃**——去重窗口内重复信号标 `deduped: true`，过滤在展示/汇总层做（与历史统计共用 dedupe.py）；并发写持线程锁；分钟/盘中信号收盘后回填 `trigger_close`；事后自动补记 **5 / 10 / 20 / 60** 交易日价格与收益；`/api/scan` 扫描结果不入档；看板新增"信号档案"面板。
**验收**：真实信号全量持久化、deduped 标记与窗口过滤正确、谨慎买入/空头平仓按修订口径落档、补记正确、汇总可用；纯本地数据。依赖：I7.1。规模：中。

### I7.3 `core-pool-manager` — 核心池管理（可配置 + 可视化 + 随时换）
看板提供核心池管理界面（从搜索/自选股勾选加入、删除、排序、重命名），持久化到后端 `data/pool.json`；池变更即用于信号日志筛选与回测快照。
**验收**：池可增删改查并持久化、version 每次变更递增（快照失效提示的完整闭环在 I7.4 快照落地后生效，I7.3 只需埋好 pool.version 关联）。依赖：I7.1。规模：中。

### I7.4 `historical-signal-stats` — 历史信号统计
核心池快照（20–40 只 + 沪深300）→ 重放式信号生成（复用 run_analysis，无前视，预热标记；**滚动截窗与实盘同构：个股最近 250 根、指数最近 60 根**；重放为原始输出、不含 app 后处理，报告头披露该口径）→ 信号去重 → 统计每个买入信号的 5/10/20/60 日 forward return（胜率/平均/期望，按股票/年份拆分）+ 可选单信号独立模拟（stop/target/无触发）。
**验收**：核心池 2–3 年可生成历史信号并可复现；无前视探针、重放窗口与实盘一致性（250/60 根）、去重、统计手算复核通过；输出 CSV + 汇总表。依赖：I7.1 + I7.3。规模：中大（一次性投入）。

### I7.5 `usability-polish` — 实用打磨
体验项：启动/加载/刷新、核心列表管理增强、信号档案导出、看板信息密度。
**验收**：清单各项落地且不回归。依赖：I7.2/I7.4。规模：中。

> 顺序理由：I7.1 打底 → I7.2 最快见效（日志先跑起来）→ I7.3 提供可维护的核心池 → I7.4 历史信号统计依赖池 → I7.5 收尾打磨。I7.2 与 I7.3 逻辑上相互独立，但**都会改动 app.py 与 dashboard/index.html**，且本流程不做分支隔离——若真并行需预期手工合并，默认建议按 I7.2 → I7.3 串行执行。

## 五、v5.0 完成定义（DoD）

1. 信号日志（5/10/20/60 视界）在真实使用中持续累积并可查看汇总；
2. 核心池可视化维护、持久化、变更可追溯（快照失效提示）；
3. 历史信号统计对核心池可运行、可复现、无前视，输出胜率/平均收益/期望/笔数（含单信号模拟，可选）；
4. 全量回归通过，全程在 Git 管理下；
5. 日常体验项完成，不引入研究级负担。

## 六、风险与开放问题

| 风险/问题 | 缓解 |
|---|---|
| 回测笔数少、置信度有限 | 报告标注笔数；信号日志长期补足 |
| 免费源历史质量 | 快照校验 + 与信号日志互相印证 |
| 自用也自欺 | 无前视 + 快照 + 真实验证三保险 |
| 核心池变更频繁 | 池版本化：变更后快照失效，重新生成才可回测 |
| 信号日志钩子影响主流程 | try/except 隔离，落盘失败只 log |
| 重放与实盘口径漂移（窗口长度/后处理差异） | 重放滚动截窗与实盘同构（250/60 根）；原始 vs 最终动作在报告头并列披露 |
| 并发请求同时触发日志钩子 | 进程内线程锁保护 journal 追加写 |

## 七、I8 迭代（2026-08-22 排定）

### I8.1 `calibration-and-fidelity` — 口径校准与统计保真

**动机**：v5 交付中披露过的口径简化（自然日近似去重、模拟无滑点/无跌停顺延/涨停无上限、统计缺离散度与小样本标注、快照无完整性校验与 stale 拒绝）在统计报告用于决策前必须收敛——"绝不自欺"的最后一公里。

**范围**：
1. `calendar.py` 升级为真实交易日工具（bar 序列即事实源）；`dedupe.py` 窗口改按交易日计数（存量 journal 的 deduped 标记不回刷，统计层实时重算立即生效）；
2. 模拟保真：滑点 0.1%（0.01 元步进双边对称）、卖出日跌停顺延（连续 5 日第 5 日收盘强平标 forced）、买入涨停顺延 5 日上限→unfilled、`timeout`/`truncated` 区分、模拟汇总表（胜率/平均·中位净收益率/盈亏比/持有天数分布）；出场口径正式采纳"盘中触价即时成交"（保守）；
3. 统计补齐：各分组标准差/标准误；n<10 渲染「样本不足」不下结论；报告输出去重前后两套汇总表；
4. 快照完整性 lite：manifest 增 bars.jsonl sha256 + config_hash；OHLC 一致性违例股票排除；replay/stats 校验 pool.version 不一致拒绝，CLI `--allow-stale` 放行并披露；
5. 分时缠论 trigger_date 回退当日遇非交易日 → 顺延至下一交易日并在 notes 标注；
6. 报告头增两项：去重窗口单位（交易日）、模拟出场口径声明。

**验收**：A1 交易日窗口修正误去重案例；A2 滑点手算一致；A3 跌停顺延与 forced 强平；A4 连续 6 日涨停→unfilled；A5 数据尾不足→truncated；A6 std/stderr 手算 + n<10 标注 + 双汇总表；A7 篡改快照拒绝 + stale 拒绝/--allow-stale 放行披露；A8 周末分时信号顺延 + 全量回归通过。

**明确不做**：组合权益模拟、bootstrap CI、自动重建调度、每日驾驶舱面板、journal 滚动归档。依赖：无新依赖。规模：中。

> I8.1 完成后：产出 D=5/10/15 去重窗口对照报告，敲定设计稿 §13 两项待确认（去重窗口默认值、池默认容量）。

### I8.2 `benchmark-excess-and-tier-monotonicity` — 超额收益与档位单调性（2026-08-29 完成归档，提交 dc0a012）

依据 `docs/迭代_i8_评估闭环/评估模块设计.md` §5.1/§5.2：stats 超额口径（基准沪深300，**自然日区间对齐**——起点 ≤ 信号日、终点 ≤ 个股视界结束日的最后指数收盘；指数缺失整体退化绝对口径并披露）+ report.md 档位单调性小节（三态标记：单调/不单调/⚠样本不足，仅披露差值与 stderr、不做显著性结论）+ 口径声明三行（基准与超额/单调性/幸存者口径）+ results.csv 增 r{h}_excess 列。
**验收**：A1 超额独立手算交叉核对（含个股停牌基准区间同步拉长、基准未覆盖缺失）；A2 无基准退化不报错；A3 单调性三态渲染+缺档注明；A4 全量回归 32/32。Verifier 43/43 通过。

### I8.3 `threshold-sensitivity` — 综合分分档阈值敏感性扫描（2026-08-29 完成归档，提交 940aabb）

依据 `docs/迭代_i8_评估闭环/评估模块设计.md` §5.3：`analysis/signal_engine.py` 分档抽为模块级 `action_from_score`（引擎与扫描单源共用，行为逐字等价）；新增 `python -m backtest sensitivity <id> --thresholds "强,买" ...`——事件集合固定（去重/warmup/forward return 一次算定，逐组仅重贴 action 标签），复用 I8.2 超额口径，输出 `results/<id>/sensitivity.md` 对照表（锚点 75,60 标识、未入选档说明、判读指引三条、不构成稳健性证明声明）。
**验收**：A1 任两组阈值事件集合一致；A2 分档边界+单源+引擎等价；A3 对照表产出且与 report.md/results.csv 隔离；A4 超额与 stats 同锚点数值一致+无基准退化；A5 全量回归 33/33。Verifier 12/12 通过。

### I8.4 `evaluation-review-loop` — 评估响应闭环 review 命令（2026-08-29 完成归档，提交 d42957e）

依据 `docs/迭代_i8_评估闭环/信号响应闭环设计.md` v1.1（用户定案：节奏 +50 笔/季度、R3 参数菜单推迟、T4 仅报告层）：新增 `python -m backtest review <id>`——对照**预先承诺**触发规则表 T1–T6（阈值集中 config），输出 `results/<id>/review.md` 逐规则状态+依据+响应菜单建议（菜单 v1={R1,R2,R4,R5}，参数类触发降级为"参数观察标记"）+ 决策日志 JSON 模板；维护 `data/decisions/review-state.json` 支持连续两次判定。**只匹配呈现不执行**（写入面仅 review.md+state）。
**验收**：A1–A5 规则逐条独立验算/状态文件/零副作用/退化报错/CLI 端到端；A6 全量回归 34/34。Verifier 24/24 通过；个人池真实数据首评无触发（默认 R4）。

### I8.5 `correction-executor` — 策略矫正执行器（2026-08-29 完成归档，提交 a90e86b）

依据 `docs/迭代_i8_评估闭环/信号响应闭环设计.md` v1.2（用户确认开通 R3 参数执行通道）：新增 `python -m backtest correct --plan <file> [--dry-run] [--rollback <action>]`——封闭菜单四类动作（pool_add/pool_remove/usage_flag/param_change），门槛从 results.csv 按 score **现算复核**（pool_remove"为负且低于池总体"双条件；param_change 两档各≥50 + r20/r60_excess×跨年方向一致 + ±5 邻域无不单调 + operator 签字），执行=备份（真 UTC 微秒）→写目标→v5.decision.v1 日志（param_change 含 decision_ref）；参数经 `data/params_override.json` 覆盖引擎分档阈值（下次进程生效，stats/sensitivity/review 报告头披露生效值）；`--rollback` 恢复备份（pool 走 pool.save version+1 原子写）。**矫正器不发明矫正**。
**验收**：第一轮 Verifier 判 fail（4 项 spec 字面偏差），修复后第二轮 23/23 通过；test_correct 11/11、全量回归 35/35。

### I8.6a `evaluation-frontend-readonly` — 评估只读前端入口（2026-08-29 完成归档，提交 b4ddd48）

依据 `docs/迭代_i8_评估闭环/前端评估入口设计.md`：看板「档案」分区新增「评估」子页签——概览卡（绝对/超额 × 总体/按动作 × 4 视界、单调性三态、生效分档阈值披露）、响应规则状态卡（T1–T6 + usage-state）、结果目录与 report/sensitivity/review 原文查看（转义防注入）；后端 3 个只读 GET 端点（server/evaluation_api.py，薄封装既有函数、walk 比对零写入）。
**验收**：第一轮 Verifier 判 fail（A19 概览卡缺按动作行），修复后第二轮 23/23 通过；test_evaluation_api 7/7、全量回归 36/36。

### I8.6b `evaluation-frontend-tasks` — 评估后台任务（2026-08-30 完成归档，提交 112b8d2）

依据 `docs/迭代_i8_评估闭环/前端评估入口设计.md` §3：新增 `POST /api/evaluation/refresh`（后台线程跑 `run_stats`+`run_review`，同一快照，池版本新鲜度与 CLI 一致）与 `POST /api/evaluation/sensitivity`（后台跑 `run_sensitivity`，阈值组由表单传入，默认锚点）；状态机复用 `_digest_state` 模式（单进程内存状态 + `data/evaluation/latest.json` 持久化，running/done/error + 阶段/进度文本，重启后 running 回填为「中断」不阻塞）；`GET /api/evaluation` 追加 `task`（进度轮询）与 `snapshots`（可生成快照列表）字段；前端评估页签增进度条与「生成评估/敏感性对照」按钮，完成后结果目录自动刷新。

**验收**：A1 缺快照/非法阈值同步拒绝不启动后台；A2 后台任务互斥（运行中重复点击忽略）;A3 状态持久化+重启回填不阻塞；A4 口径不旁路（stats/sensitivity 同样传入 expected_pool_version，无 --allow-stale 前端旁路）；A5 全量回归 37/37。



###I8.6c `correction-frontend` — 矫正计划前端入口（2026-08-30 完成归档，提交 112b8d2）

依据 `docs/迭代_i8_评估闭环/前端评估入口设计.md` §3/§4：新增 `POST /api/correct/validate`（计划落盘 `data/decisions/plans/` 作传输与审计留痕，dry-run 只校验门槛零写目标）与 `POST /api/correct/execute`（按 plan_id 复用同一份计划，补 operator 签字 + confirmed 二次确认后走 `correct.py` 同一 `run_correct` 代码路径，门槛执行侧现算复核，**无前端绕过路径**；返回执行摘要、决策日志行号与 `--rollback` 提示）；前端矫正计划表单（动作/载荷/证据快照/触发规则）+ 门槛 PASS/FAIL 逐条展示 + 签字执行区。

**验收**：A1 validate 门槛 PASS/FAIL 与 CLA 同判；A2 dry-run 零写目标（pool/override/usage/decision log 均不动）;A3 execute 缺 operator/confirmed/plan_id 全拒绝；A4 重复使用同一 plan_id 执行留痕可追溯；A5 全量回归 37/37（test_correct_frontend 6/6）。

## 八、I9 迭代（2026-09-01 排定，同日完成归档）— 选股层与滚动评估

主线：**选股层管线**（扫描 → 候选池 → 无前视验证 → 建议单 → 人工拍板入池/出池）+ 前置**月度滚动评估**；组合模拟押后至 v6 条件项。直接依据评估基线（2026-08-29）三条发现：超额口径反转读法、α 高度依赖选股、强买档样本不足 + 2026 环境转差。完整设计（数据事实锚点、门槛预承诺、逐迭代验收、风险表、DoD）见 `docs/迭代_i9_选股层/选股层与滚动评估设计.md`。

### I9.0 `task-store-unification` — 状态收敛前置（提交随 I9 一起归档）

三套同构状态读写收敛为 `server/task_store.py`：统一落盘 `data/tasks/<kind>.json`（kind=scan|digest|notify|screen），旧路径迁移读保留不删；`/api/health` 改走统一 read_state；新增 `/api/tasks` 只读聚合。**验收**：迁移读/损坏回填/原子写/并发写锁/旧文件保留全绿（test_task_store 7/7 + test_state_persist 改造后全绿）。

### I9.1 `rolling-evaluation` — 月度滚动评估

`server/rolling_eval_service.py`：每交易日 15:45 例行自检（`ROLLING_EVAL_AT`），**当月未跑且交易日才触发**（幂等键=月份），snapshot→replay→stats→review 一条龙；每期摘要 append `data/evaluation/index.jsonl`，手动「生成评估」同函数落行；评估页签「历史趋势」逐期对比；与手动评估/候选验证共用单任务互斥。**验收**：注入时钟的 should_run 分支、成功/失败落行、幂等、互斥、重启补跑全绿（test_rolling_eval 7/7）。

### I9.2 `screener-candidates` — 候选池

`backtest/candidates.py` + `/api/candidates`：`data/candidates.json`（v5.candidates.v1），变更原语与 pool 同语义；状态机 watching/validated/parked/promoted/rejected；`promoted/rejected` 后 20 交易日冷却（指数日K日历，失败回退自然日）；容量上限 30；核心池结构零改动。**验收**：add/幂等/容量/来源校验/冷却/重激活/导入/API 全绿（test_candidates 10/10）。

### I9.3 `candidate-validation` — 候选历史验证

`backtest/screen.py`：候选快照（manifest 增 `source:"screen"`/`candidates_version`）→ 无前视重放 → 统计 → `screen.md`+`screen.csv`；**SCREEN_GATE**（买入侧合计，n≥10、r20/r60_excess>0、双超额胜率≥50%，样本不足永不 PASS）；验证成功候选置 validated。**验收**：门槛四条逐条验算、样本不足抑制、产物/状态回写/stale 透传全绿（test_screen 7/7）。

### I9.4 `pool-advisor` — 入池/出池建议

`backtest/advise.py` + `/api/advice`：screen.csv PASS→`pool_add` 草稿；评估 results.csv 逐股滚动超额跌破（负）且逐股样本≥10→`pool_remove` 草稿；草稿写 `data/decisions/plans/` 可被 correct 消费；**建议器零写池/override**；`pool_add` 执行成功由 `correct.py` 统一回写候选 `promoted`（P27）。**验收**：add/remove 草稿、样本门槛观察、已入池跳过、只读 API、零写池、promoted 回写全绿（test_advise 6/6）。

### I9.5 `screener-frontend` — 候选与建议前端入口

看板「档案 → 候选」页签：候选列表/增删改注/扫描一键导入/验证后台任务（`/api/candidates/validate`，状态经 task_store kind=screen，重启回填中断）+ 进度轮询；建议单只读卡 + 跳转矫正页签；评估页签「历史趋势」series 渲染。**验收**：后台任务互斥/进度/重启回填全绿（test_candidate_validate 2/2），前端接线守卫通过。

## 九、模拟账户 v6→v8（2026-09-01 起陆续落地）

组合模拟（评估模块设计 §5.5 条件项）落地为**模拟账户**：虚拟资金 + 策略自动选股与买卖 +
记账 + 组合级绩效。设计稿 `docs/迭代_v6-v8_模拟账户/模拟账户设计.md`，Comet 归档 `docs/comet/archive/`。

- **V6.1 `sim-account`**：看板「模拟」分区（概览 / 组合指标 / 净值曲线 / 持仓 / 流水 / 配置）；
  `GET|POST /api/sim`（save / run_once / reset / buy / sell）；交易时段自动巡检
  （默认关闭）；**账户与策略解耦**（`Decision` 契约 + `StrategyAdapter` 可插拔，
  当前 `qushi_v5`；`UniverseProvider` 支持全 A 扫描 / 自选 / 核心池）；
  成交口径与 `stats.simulate_signal` 同源（滑点 / 费率 / 整手 / T+1 / 涨跌停顺延）；
  模拟成交不写 `data/journal/`。
  **验收**：A1–A16 全过（含解耦、涨跌停顺延、资金守恒、绩效手算）；回归 49/49 全绿。

- **V6.2 `sim-notify`**（模拟操作推钉钉）：模拟账户实际成交（买入/卖出/止损/止盈/超期/手动/强制）实时推送到钉钉，
   配置块 `data/sim/config.json→notify`（enabled/webhook/secret/ops）；以成交流水 `trade.id` 去重（`data/sim/notify_sent.json`），
   失败不阻塞巡检；复用 `server/notify_service.py` 发送机制。

- **V6.3 `external-rule-fusion`**（2026-09 外源参考机制融合）：依据 `workspace_tmp/ashare-quant-strategies/`
   评审结论（119 篇），融合「只取机制、不取结论」；设计稿 `docs/策略融合-外源参考-2026-09.md`。
   - **RSRS 大盘门控**（084/101 参考）：适配层策略参数 `rsrs_gate`（**默认关**）+ `rsrs_threshold`/`rsrs_bear_action`；
     N=18/M=250（数据源单次 400 根上限，M 从参考值 600 调整，留痕）参数进 config（`SIM_RSRS_*`）；只作用于模拟账户买入 Decision（hold/降级），
     不触碰 SCREEN_GATE / 建议单 / 信号引擎评分；
   - **动态退出规则**（115/107/022/078 参考）：`StrategyAdapter.exit_check` 可选接口 + 服务层接入；
     顺序 涨停开板 → 破 MA20 → 高点回撤 3% → 放量 3×10 日均量，**默认开**，参数进 config（`SIM_EXIT_*`）；
   - **动量披露字段**（043/003 参考）：`MomentumResult.momentum_quality / market_rsrs`（**不参与总分**，
     评估/回测/档案口径零影响）；`analysis/_indicators.py` 新增 `linfit_stats / zscore_last / rsrs_score`；
   **验收**：新测试 4 文件（indicators_rsrs / momentum_disclose / sim_exit_rules / sim_rsrs_gate）全绿 +
     全量回归全绿；账户内核 `sim_account.py` 保持零 `analysis` 依赖（解耦 Spec §1.1）。

- **V6.4 `fusion-phase2`**（2026-09-03 外源融合第二阶段）：设计稿 `docs/策略融合-第二阶段设计-2026-09.md`；
   按用户拍板执行 A（基本面因子）+B（B 级行业动量）+C（撮合排队 volume 代理）。
   - **A 基本面因子**：`backtest/factors.py`（东财 stock/get 直读 f164/f167/f116/f117/f187；
     股息率=分红率÷PE-TTM、ROE=PB÷PE-TTM 恒等式推导，5 只样本实测验证）；候选池 item 可选 `factor`
     （schema `v5.candidates.v1` 不升、旧文件兼容）；建议单 evidence 披露 `factor/factor_score/factor_error`；
     不进 SCREEN_GATE/信号引擎/screen.csv/核心池；不做 PEG；
   - **B 行业动量 B 级**：`backtest/industry_momentum.py` 池内 ≥2 只按 r60_excess 聚合排名，随建议单披露；
     口径「池内·60日超额」（设计稿「60 日涨跌幅」修正：与回测行级口径一致、零新数据源）；参数 `INDUSTRY_MOM_*`；
   - **C 撮合排队**：`QushiV5Adapter.queue_check` volume 代理（默认 off 零影响；`SIM_QUEUE_*` 参数）；
     queue_pending 复用 `_track_pending` 顺延（kind 区分、超 EXIT_POSTPONE_LIMIT unfilled+披露，成交 note `queue-deferred`）；
     账户内核仅 `execute_buy` 增可选 note 参数；
   **验收**：新测试 3 文件（factors / industry_momentum / sim_queue）全绿 + 全量回归全绿（63/63）；
     建议器零写池、SCREEN_GATE/评分公式零改动。单 change 归档 `docs/comet/archive/phase2-strategy-fusion/`。

- **V6.5 `sim-exec-close-nextday`**（提交 a114598；原编号 V6.3，与外源融合 V6.3 撞号，2026-09-04 双线合并重编）：信号执行模式 close_nextday 落地——收盘定档
  （15:05）+ 次日执行清单，日线信号不再盘中重算；intraday 模式可回退；顺带修复涨停顺延计数丢失 bug。

- **V7 `sim-config-decouple`**（提交 8af4859，设计稿 `docs/迭代_v6-v8_模拟账户/模拟账户解耦-下一代迭代设计.md`）：
  配置与策略解耦——配置拆「账户参数 + strategy_params」两层；adapter 自描述参数 schema
  （`params_schema`/`normalize_params`）+ 前端按 schema 动态渲染 + v1 配置自动迁移 + 孤儿键清理；
  选股错峰治理（避开 KLINE_SYNC 窗口，连续被 WAF 拦截提前终止本轮选股并标 `source_throttled`）。

- **V8 `sim-benchmark`**（提交 c61e5de，设计稿 `docs/迭代_v6-v8_模拟账户/模拟账户v8-大迭代设计.md`）：
  全自动机器人 + 基准对比——基准可选沪深300（000300，默认）/中证500（000905），`equity.jsonl` 行增
  benchmark 字段；新增**超额年化 / 超额最大回撤 / 信息比率**（超额样本≥20 才披露，样本不足沿用披露纪律）；
  净值曲线叠加基准线；策略下拉切换（schema 驱动重渲染）；快照带基准。

### 配套与工程小迭代（陆续落地）

- 模拟账户独立大页 + 顶栏一级入口（6abe6de / 47fb66f）；
- 扫描展示「被策略门拦截」候选——原始动作达买入档但被市场/趋势门压住的股票单独列出，含拦截原因（7fc9887）；
- 前端动线复核修复：sim 重置审计分区 / 深链跳过首访引导 / 空池禁用候选验证（8ddfe5a）；
- **发现全自动（拍板 2026-09-04）**：扫描完成后 found（前20）+ 被策略门拦截的候选自动入候选池（`source=scan`，幂等去重、上限约束、`SCAN_AUTO_CANDIDATE=0` 可关）；拦截组入池为策略门价值评估提供固定证据集；候选→核心池的人工拍板闸门不变；
- **watching 过期自动搁置（拍板 2026-09-04）**：watching 超 20 个交易日未处理 → 扫描时自动 parked（留痕可复活）；容量上限改为只数活跃态，池子不会再被老条目卡满；
- **screen FAIL 保持 watching + 状态机审计（拍板 2026-09-04）**：只有 PASS 置 validated，FAIL 留队列自动重试；所有状态迁移写 `candidates_audit.jsonl`（含 actor），此前一次 screen 把 18 只 FAIL 也置了 validated 且 600036 出现无来源 promoted——已回退并对账；
- 钉钉推送全面迁移新版企业内部应用机器人 OpenAPI（旧版自定义机器人 webhook 被官方宣布下线）（1ef440f）。

剩余展望（条件性，不承诺，按优先级）：
1. **口径收敛**：最终动作纳入重放并标记 policy 版本——第一性原则表唯一「待下一轮」项，评估可信度的前提（**I10 已实现**，见 `docs/迭代_i10_口径收敛/口径收敛设计.md` 与 `tests/test_policy_replay.py`）；
2. **证据积累**：滚动评估连续数期，观察策略门拦截信号与分档表现；若过滤无稳定增量，按第一性原则 §5 撤回策略门或把工具降级为技术分析看板；
3. ~~**持仓级净值归因**（哪只股票 / 哪个档位贡献或拖累超额）~~ **已实现（2026-09-04，I11）**：模拟账户按标的/按档位拆解已实现+浮动贡献（`sim_account.contribution_summary` + 看板「贡献拆解」卡），纯披露不设门槛；超额归因单笔口径未覆盖，见 `docs/迭代_i11_持仓归因/持仓级净值归因设计.md`；
4. **多策略并行与 A/B 归因**（架构已预留，样本充足前不实施）；
5. 扩大核心池 / 维持维护；
6. **趋势刻画增强（数据驱动，I10 之后）**：按 direction/strength 分桶判别力证据决定——候选方向：方向判定引入 MA60/斜率/趋势持续时间、strength 由二值开关连续化、剔除 resonance 与 MA20 方向的重复计分；证据不支持前不动；
7. ~~前端小项：看板目标价处标注来源~~ **已完成（2026-09-04）**：四问卡目标价 box 与「目标依据」行展示 `trade_plan.target_source`（形态量度 / 箱体上沿 / 经验+10% 估算，旧数据无该字段显示 --）。

## 十、I10 迭代（2026-09-02 排定）— 口径收敛：最终动作纳入重放

**动机**：看板/扫描/推送/模拟账户均使用最终动作（含策略门等后处理），而回测/评估/SCREEN_GATE 仍用原始动作——被评估对象 ≠ 使用对象，第一性原则 §2 末行「待下一轮」项。
**主线**：后处理单源化为纯函数（行为冻结迁移）→ 重放双口径落盘（raw_action/final_action + policy_version/policy_hash）→ 统计双口径并列披露 + 拦截分析小节 → 旧结果 legacy 隔离 → 缓存按 policy_hash 失效。
**状态**：**已实现（2026-09-04）**——A1–A8 落地，全量回归 57/57 绿（新增 `tests/test_policy_replay.py`）。三项拍板：Q1=SCREEN_GATE/advise 切换最终口径（决策留痕 `data/decisions/log.jsonl` I10-Q1）；Q2=`policy.v1.gate`；Q3=sensitivity 维持 raw 锚、双标签（锚点组用落盘 final_action，非锚点组由 policy_inputs 复算、不含盈亏比否决并披露）。
