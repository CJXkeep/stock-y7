# 候选与建议前端入口（dashboard）

## 概述

看板提供选股管线的可视化入口：候选池维护、候选验证触发与进度、建议单查看，以及评估页签的历史趋势。本 capability 归档后，选股管线从"只有 CLI"变为日常可用；前端**不新增任何执行通道**，池变更仍只能经既有矫正页签完成。

## 行为

### 候选页签

- 候选列表：symbol / 名称 / 行业 / 状态 / 来源 / 首次发现动作与分数 / 最近验证结果（PASS|FAIL 与不达标原因）；
- 操作：手动添加、删除、备注内联编辑、状态切换、批量导入；
- 扫描结果一键「加入候选」（沿用扫描结果字段，重复/冷却/超容时展示对应提示）。

### 候选验证

- `POST /api/candidates/validate`：后台线程跑 `backtest/screen`，与评估后台任务**共用同一单任务互斥**；
- 进度条轮询（沿用评估任务状态机：running/done/error + 阶段文本 + 进度）；
- 状态持久化到 `data/tasks/`（沿用 I9.0 的 task_store）；进程重启后 running 回填为"中断"，不阻塞新任务；
- 完成后候选列表自动刷新验证结果列。

### 建议卡

- 展示最新建议单摘要：动作、标的、证据快照、四视界数值、门槛逐条 PASS/FAIL；
- 提供「去矫正页签执行」跳转——**执行仍走既有矫正表单**，本页签只读，无任何绕过路径。

### 评估页签「历史趋势」

- 读取 `GET /api/evaluation` 的 `series` 字段，渲染逐期总体超额折线与触发规则变迁列表；
- 空态（无历史期数）给出解释文案：需等待累积 ≥2 期；
- 标注该序列为**原始输出统计口径**，与信号档案的最终 action 口径不可混用。

## 不变式

- 前端无构建步骤（原生 ESM），沿用 `dashboard/js/` 现有结构与事件委托约定（含 `data-act` 委托放行）；
- 所有按钮必须有对应端点，无死链（`tests/test_frontend_wiring.py` 守护）；
- 单进程约束不变：前端不得假设多副本可用；
- 不新增池变更执行通道，不绕过 operator 签字与二次确认。

## 降级与失败处理

- 验证任务失败 → 展示失败阶段与原因，可重试；
- 后端不可达/返回错误 → 页签内提示，不影响其他页签；
- 候选池为空 → 空态引导文案（可从扫描结果或手动添加开始）。

## 验收映射

P28、P29、P30、P31、P32、P33。
