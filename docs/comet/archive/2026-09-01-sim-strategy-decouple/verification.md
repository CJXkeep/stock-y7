---
generated_from_state_version: 9
---

# Verification

## Current result

- Result: **Passed**
- Assurance: **skill-coordinated**
- Goal cycle: 2
- Iteration: 1
- Verifier attempt: 1
- Completed: 2026-09-01T13:12:59.105Z
- Summary: 独立验收：逐项读取 brief/spec/实现与测试。A1-A6 全部通过（部分保存、迁移幂等、schema 契约与回退、前端动态渲染、错峰推迟可见、限流提前终止）；A7-A57 覆盖三个 capability 的全部可执行语义。Constraints 未违反：账户内核不 import 策略层、Decision 契约字段未变、单轮互斥/原子写/损坏回退/样本不足披露纪律保持。Runtime 检查：pytest 3 文件 37/37、run_all_tests 51/51。3 项低风险已记录。

## Acceptance

| ID | Result | Source | Criterion | Reason |
| --- | --- | --- | --- | --- |
| A1 | passed | brief.md | **A1** 部分保存只更新提供字段：对 v7 配置仅提交 `interval_min`，返回配置中账户参数与 `strategy_params` 均保持原值，`version` 递增；对 `strategy_params` 的子集提交同样只更新提供的键。 | A1 部分保存：normalize_config 以 current 为底键级子合并；test_config_partial_save_keeps_current 与 test_save_config_preserves_current_on_partial_save 覆盖；运行时 API 冒烟实测部分保存仅更新提供字段 |
| A2 | passed | brief.md | **A2** v1 旧配置自动迁移：构造一份 v1 配置文件（含 buy_levels/min_score/level_scale/require_weekly），首次读取后得到 v7 结构，策略字段进入 `strategy_params`、无丢失、version 递增一次，且迁移是幂等的（再次读取不再递增）。 | A2 v6→v7 迁移：_migrate_v6_config 仅当 schema==v6 触发、策略键移入 strategy_params、version+1 原子写回；test_config_v6_migration 验证迁移与幂等 |
| A3 | passed | brief.md | **A3** adapter 自描述：`GET /api/sim` 返回 `strategy_schema`（当前 adapter 的参数 schema）；换注册一个声明不同 schema 的假 adapter 后，接口返回其 schema，非法 `strategy` 值回退默认 adapter 并在 state 告警。 | A3 adapter 自描述：get_adapter 返回注册表实例并透传 strategy_params，params_schema/normalize_params 在 QushiV5Adapter 与假 adapter 上均有测试（test_params_schema_and_normalize、test_fake_adapter_registry_schema）；未知策略回退 qushi_v5 并在 run_cycle 中写 state.last_error 告警 |
| A4 | passed | brief.md | **A4** 前端动态渲染：`sim.html` 与侧边栏窄栏的「策略参数」区由 `strategy_schema` 渲染（含 label/默认值/边界），修改参数保存后生效；账户参数区仍为固定字段。 | A4 前端动态渲染：_renderStrategyParams 按 strategy_schema 渲染 bool/enum/int/float/未知类型（label/default/min/max 生效），sim.html 与 index.html 均含 #sim-strategy-params 容器；保存以 strategy_params 提交；接口失败显示空态不影响账户参数区 |
| A5 | passed | brief.md | **A5** 选股错峰：在模拟的 K 线同步窗口内触发的选股被推迟到窗口之后，state 可见推迟原因；窗口外选股照常执行。 | A5 错峰：_in_sync_window 按 KLINE_SYNC_AT+SIM_SYNC_WINDOW_MIN 判定；窗口内 _maybe_screen 跳过选股并写 screen_deferred（含下次可选时间），force 绕过；test_sim_offpeak 覆盖窗口边界/推迟/force |
| A6 | passed | brief.md | **A6** WAF 提前终止：模拟连续 N 只候选行情被拦截的场景，本轮选股提前终止，state 标记 `source_throttled`，其余轮次不受影响。 | A6 限流终止：screen 连续 evaluate 失败达 abort_threshold 抛 SourceThrottledError，_maybe_screen 捕获后标记 source_throttled、丢弃部分结果、不产生买入；test_screen_aborts_on_consecutive_source_failures / test_screen_survives_isolated_failures 覆盖 |
| A7 | passed | specs/screen-offpeak/spec.md | 归档后 capability 的完整行为规格。 | screen-offpeak spec 语义已实现：KLINE_SYNC_AT 常量、SYNC_WINDOW_MIN 默认15分钟、窗口内跳过选股(持仓巡检与净值快照照常)、state 记录推迟原因、force 绕过、SCREEN_ABORT_THRESHOLD(默认20)、提前终止标记 source_throttled 且为单轮状态(下次选股开始清除)、部分初筛结果不用于买入 |
| A8 | passed | specs/screen-offpeak/spec.md | K 线同步窗口 = `KLINE_SYNC_AT`（当前 15:30）起的 `SYNC_WINDOW_MIN`（实现常量，默认 15 分钟）； | screen-offpeak spec 语义已实现：KLINE_SYNC_AT 常量、SYNC_WINDOW_MIN 默认15分钟、窗口内跳过选股(持仓巡检与净值快照照常)、state 记录推迟原因、force 绕过、SCREEN_ABORT_THRESHOLD(默认20)、提前终止标记 source_throttled 且为单轮状态(下次选股开始清除)、部分初筛结果不用于买入 |
| A9 | passed | specs/screen-offpeak/spec.md | 选股调度触发时若处于同步窗口内：本轮**跳过选股**（持仓巡检与净值快照照常执行），state 记录推迟原因（含下次可选时间）； | screen-offpeak spec 语义已实现：KLINE_SYNC_AT 常量、SYNC_WINDOW_MIN 默认15分钟、窗口内跳过选股(持仓巡检与净值快照照常)、state 记录推迟原因、force 绕过、SCREEN_ABORT_THRESHOLD(默认20)、提前终止标记 source_throttled 且为单轮状态(下次选股开始清除)、部分初筛结果不用于买入 |
| A10 | passed | specs/screen-offpeak/spec.md | `run_once force=true` 不受窗口限制（手动触发视为知情操作）。 | screen-offpeak spec 语义已实现：KLINE_SYNC_AT 常量、SYNC_WINDOW_MIN 默认15分钟、窗口内跳过选股(持仓巡检与净值快照照常)、state 记录推迟原因、force 绕过、SCREEN_ABORT_THRESHOLD(默认20)、提前终止标记 source_throttled 且为单轮状态(下次选股开始清除)、部分初筛结果不用于买入 |
| A11 | passed | specs/screen-offpeak/spec.md | 选股初筛阶段连续 `SCREEN_ABORT_THRESHOLD`（实现常量，默认 20）只候选因行情源拦截（WAF/限流类错误）而失败时，提前终止本轮选股； | screen-offpeak spec 语义已实现：KLINE_SYNC_AT 常量、SYNC_WINDOW_MIN 默认15分钟、窗口内跳过选股(持仓巡检与净值快照照常)、state 记录推迟原因、force 绕过、SCREEN_ABORT_THRESHOLD(默认20)、提前终止标记 source_throttled 且为单轮状态(下次选股开始清除)、部分初筛结果不用于买入 |
| A12 | passed | specs/screen-offpeak/spec.md | 终止时 state 标记 `source_throttled=true`（含终止时已评估数量），本轮不产生买入； | screen-offpeak spec 语义已实现：KLINE_SYNC_AT 常量、SYNC_WINDOW_MIN 默认15分钟、窗口内跳过选股(持仓巡检与净值快照照常)、state 记录推迟原因、force 绕过、SCREEN_ABORT_THRESHOLD(默认20)、提前终止标记 source_throttled 且为单轮状态(下次选股开始清除)、部分初筛结果不用于买入 |
| A13 | passed | specs/screen-offpeak/spec.md | 标记为单轮状态：下一轮选股开始时清除； | screen-offpeak spec 语义已实现：KLINE_SYNC_AT 常量、SYNC_WINDOW_MIN 默认15分钟、窗口内跳过选股(持仓巡检与净值快照照常)、state 记录推迟原因、force 绕过、SCREEN_ABORT_THRESHOLD(默认20)、提前终止标记 source_throttled 且为单轮状态(下次选股开始清除)、部分初筛结果不用于买入 |
| A14 | passed | specs/screen-offpeak/spec.md | 已产生的部分初筛结果不用于买入（避免样本截断偏差）。 | screen-offpeak spec 语义已实现：KLINE_SYNC_AT 常量、SYNC_WINDOW_MIN 默认15分钟、窗口内跳过选股(持仓巡检与净值快照照常)、state 记录推迟原因、force 绕过、SCREEN_ABORT_THRESHOLD(默认20)、提前终止标记 source_throttled 且为单轮状态(下次选股开始清除)、部分初筛结果不用于买入 |
| A15 | passed | specs/sim-config-decouple/spec.md | 归档后 capability 的完整行为规格。 | screen-offpeak spec 语义已实现：KLINE_SYNC_AT 常量、SYNC_WINDOW_MIN 默认15分钟、窗口内跳过选股(持仓巡检与净值快照照常)、state 记录推迟原因、force 绕过、SCREEN_ABORT_THRESHOLD(默认20)、提前终止标记 source_throttled 且为单轮状态(下次选股开始清除)、部分初筛结果不用于买入 |
| A16 | passed | specs/sim-config-decouple/spec.md | 模拟账户配置分两层： | screen-offpeak spec 语义已实现：KLINE_SYNC_AT 常量、SYNC_WINDOW_MIN 默认15分钟、窗口内跳过选股(持仓巡检与净值快照照常)、state 记录推迟原因、force 绕过、SCREEN_ABORT_THRESHOLD(默认20)、提前终止标记 source_throttled 且为单轮状态(下次选股开始清除)、部分初筛结果不用于买入 |
| A17 | passed | specs/sim-config-decouple/spec.md | **账户/引擎参数**（与策略无关，永远有效）：`enabled`、`initial_capital`、`universe`、`scan_limit`、`interval_min`、`screening_interval_min`、`max_positions`、`per_trade_pct`、`max_hold_days`、`auto_sell`、`stop_loss_enabled`、`take_profit_enabled`、`strategy`（策略选择字段）。 | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A18 | passed | specs/sim-config-decouple/spec.md | **策略参数**（`strategy_params`，不透明字典）：由当前 `strategy` 对应的 adapter 声明 schema 并归一化校验；后端与账户内核不解释其内部键。 | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A19 | passed | specs/sim-config-decouple/spec.md | 配置 schema 标识为 `v7.sim.config.v1`。 | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A20 | passed | specs/sim-config-decouple/spec.md | `normalize_config(data, current)`： | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A21 | passed | specs/sim-config-decouple/spec.md | 以 current 为底（current 是已归一化的合法配置），data 覆盖其提供的顶层键； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A22 | passed | specs/sim-config-decouple/spec.md | 账户参数按既有规则归一化（范围夹取、非法回退默认）； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A23 | passed | specs/sim-config-decouple/spec.md | `strategy_params` 为 dict 时按「键级子合并」：只更新 data 提供的键，其余键沿用 current；非 dict 回退 current 或 `{}`； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A24 | passed | specs/sim-config-decouple/spec.md | `strategy_params` 的最终合法性由当前策略 adapter 的 `normalize_params()` 归一化——发生在 adapter 实例化时（`get_adapter` 以 `strategy_params` 构造 adapter）；账户内核不 import 策略层，故不在此处归一化； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A25 | passed | specs/sim-config-decouple/spec.md | 部分保存只更新提供的字段，`version` 递增，`updated_at` 刷新（原子写）。 | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A26 | passed | specs/sim-config-decouple/spec.md | 读取到 `schema == "v6.sim.config.v1"` 的配置文件时： | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A27 | passed | specs/sim-config-decouple/spec.md | 顶层策略专属键 `buy_levels`、`level_scale`、`min_score`、`require_weekly` 移入 `strategy_params`，从顶层删除； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A28 | passed | specs/sim-config-decouple/spec.md | 其余顶层键映射为 v7 账户参数（名称不变）； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A29 | passed | specs/sim-config-decouple/spec.md | `schema` 置为 `v7.sim.config.v1`，`version` 递增一次，原子写回； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A30 | passed | specs/sim-config-decouple/spec.md | 迁移幂等：已是 v7 的配置不再触发迁移、不再递增 version； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A31 | passed | specs/sim-config-decouple/spec.md | 迁移失败（文件损坏等）回退默认 v7 配置并告警，服务照常启动（沿用既有纪律）。 | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A32 | passed | specs/sim-config-decouple/spec.md | `StrategyAdapter` 新增： | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A33 | passed | specs/sim-config-decouple/spec.md | `QushiV5Adapter`： | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A34 | passed | specs/sim-config-decouple/spec.md | `params_schema()` 声明 `buy_levels`（enum 多选：strong/normal/cautious）、`min_score`（int 0–100）、`require_weekly`（bool）、`scale_strong` / `scale_normal` / `scale_cautious`（float 0–1，档位仓位系数按档位拆为三个标量参数，便于通用前端渲染）； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A35 | passed | specs/sim-config-decouple/spec.md | 策略专属买入过滤（`buy_levels` 档位过滤、`min_score` 综合分下限）由 adapter 完成：`screen()` 只返回通过策略过滤的可买 `Decision`； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A36 | passed | specs/sim-config-decouple/spec.md | 档位→仓位缩放经 adapter 暴露的 `position_scale(level) -> float`（默认 1.0）提供给服务层，账户层不解释档位名； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A37 | passed | specs/sim-config-decouple/spec.md | 未识别的 `strategy` 值回退默认 adapter（qushi_v5），并在 task state 的 `last_error` 告警一次。 | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A38 | passed | specs/sim-config-decouple/spec.md | `GET /api/sim` 契约新增字段： | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A39 | passed | specs/sim-config-decouple/spec.md | `strategy_schema`：当前 adapter 的 `params_schema()` 结果（供前端动态渲染）； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A40 | passed | specs/sim-config-decouple/spec.md | `strategy_params`：当前生效的策略参数值。 | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A41 | passed | specs/sim-config-decouple/spec.md | `POST /api/sim action=save`： | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A42 | passed | specs/sim-config-decouple/spec.md | 账户参数与 `strategy_params` 可在同一请求中混合提交； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A43 | passed | specs/sim-config-decouple/spec.md | `strategy_params` 先键级子合并，再经 adapter `normalize_params()` 归一化。 | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A44 | passed | specs/sim-config-decouple/spec.md | 账户内核 `backtest/sim_account.py` 不 import `server/sim_strategy.py`； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A45 | passed | specs/sim-config-decouple/spec.md | `Decision` 契约字段不变； | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A46 | passed | specs/sim-config-decouple/spec.md | 巡检单轮互斥、单进程部署、配置原子写、损坏回退默认并告警、样本不足披露纪律均保持。 | sim-config-decouple spec 语义已实现：v7 两层结构、账户参数归一化、strategy_params 键级子合并、adapter 实例化时 normalize_params 二次归一化、v6 自动迁移幂等、params_schema 声明 buy_levels/min_score/require_weekly/scale_*(float 0-1)、策略过滤移入 screen、position_scale 提供仓位缩放、get_adapter 注册表回退、GET 返回 strategy_schema/strategy_params、POST 混合提交；不变式(账户内核不 import 策略层、Decision 契约不变、原子写/损坏回退)未违反 |
| A47 | passed | specs/sim-frontend-dynamic/spec.md | 归档后 capability 的完整行为规格。 | sim-frontend-dynamic spec 语义已实现：账户参数固定区+策略参数动态区、bool开关/enum复选/int-float数字框(带min/max)/未知类型退化为文本输入并保留原值、label 展示名 default 兜底、保存读取为 strategy_params、缺 schema 时策略参数区空态提示不影响账户参数区 |
| A48 | passed | specs/sim-frontend-dynamic/spec.md | 适用 `/sim.html` 大页与看板侧边栏窄栏（共用同一渲染逻辑）： | sim-frontend-dynamic spec 语义已实现：账户参数固定区+策略参数动态区、bool开关/enum复选/int-float数字框(带min/max)/未知类型退化为文本输入并保留原值、label 展示名 default 兜底、保存读取为 strategy_params、缺 schema 时策略参数区空态提示不影响账户参数区 |
| A49 | passed | specs/sim-frontend-dynamic/spec.md | 配置面板分「账户参数」（固定字段，同现状）与「策略参数」（由 `strategy_schema` 动态生成）两区； | sim-frontend-dynamic spec 语义已实现：账户参数固定区+策略参数动态区、bool开关/enum复选/int-float数字框(带min/max)/未知类型退化为文本输入并保留原值、label 展示名 default 兜底、保存读取为 strategy_params、缺 schema 时策略参数区空态提示不影响账户参数区 |
| A50 | passed | specs/sim-frontend-dynamic/spec.md | 动态渲染规则： | sim-frontend-dynamic spec 语义已实现：账户参数固定区+策略参数动态区、bool开关/enum复选/int-float数字框(带min/max)/未知类型退化为文本输入并保留原值、label 展示名 default 兜底、保存读取为 strategy_params、缺 schema 时策略参数区空态提示不影响账户参数区 |
| A51 | passed | specs/sim-frontend-dynamic/spec.md | `type=bool` → 开关（checkbox）； | sim-frontend-dynamic spec 语义已实现：账户参数固定区+策略参数动态区、bool开关/enum复选/int-float数字框(带min/max)/未知类型退化为文本输入并保留原值、label 展示名 default 兜底、保存读取为 strategy_params、缺 schema 时策略参数区空态提示不影响账户参数区 |
| A52 | passed | specs/sim-frontend-dynamic/spec.md | `type=enum` 且提供 `options` → 复选组或下拉； | sim-frontend-dynamic spec 语义已实现：账户参数固定区+策略参数动态区、bool开关/enum复选/int-float数字框(带min/max)/未知类型退化为文本输入并保留原值、label 展示名 default 兜底、保存读取为 strategy_params、缺 schema 时策略参数区空态提示不影响账户参数区 |
| A53 | passed | specs/sim-frontend-dynamic/spec.md | `type=int` / `type=float` → 数字输入（应用 `min` / `max` 边界）； | sim-frontend-dynamic spec 语义已实现：账户参数固定区+策略参数动态区、bool开关/enum复选/int-float数字框(带min/max)/未知类型退化为文本输入并保留原值、label 展示名 default 兜底、保存读取为 strategy_params、缺 schema 时策略参数区空态提示不影响账户参数区 |
| A54 | passed | specs/sim-frontend-dynamic/spec.md | `label` 作为展示名，`default` 作为空值兜底； | sim-frontend-dynamic spec 语义已实现：账户参数固定区+策略参数动态区、bool开关/enum复选/int-float数字框(带min/max)/未知类型退化为文本输入并保留原值、label 展示名 default 兜底、保存读取为 strategy_params、缺 schema 时策略参数区空态提示不影响账户参数区 |
| A55 | passed | specs/sim-frontend-dynamic/spec.md | 渲染对未知 `type` 容错：退化为文本输入并保留原值； | sim-frontend-dynamic spec 语义已实现：账户参数固定区+策略参数动态区、bool开关/enum复选/int-float数字框(带min/max)/未知类型退化为文本输入并保留原值、label 展示名 default 兜底、保存读取为 strategy_params、缺 schema 时策略参数区空态提示不影响账户参数区 |
| A56 | passed | specs/sim-frontend-dynamic/spec.md | 保存时把策略参数区读为 `strategy_params` 对象随 `action=save` 提交； | sim-frontend-dynamic spec 语义已实现：账户参数固定区+策略参数动态区、bool开关/enum复选/int-float数字框(带min/max)/未知类型退化为文本输入并保留原值、label 展示名 default 兜底、保存读取为 strategy_params、缺 schema 时策略参数区空态提示不影响账户参数区 |
| A57 | passed | specs/sim-frontend-dynamic/spec.md | 接口失败或 `strategy_schema` 缺失时，策略参数区显示为空态提示，不影响账户参数区可用。 | sim-frontend-dynamic spec 语义已实现：账户参数固定区+策略参数动态区、bool开关/enum复选/int-float数字框(带min/max)/未知类型退化为文本输入并保留原值、label 展示名 default 兜底、保存读取为 strategy_params、缺 schema 时策略参数区空态提示不影响账户参数区 |

## Checks

| Check | Command | Working directory | Status | Exit | Duration |
| --- | --- | --- | --- | ---: | ---: |
| sim 相关单元/回归测试 | -m pytest tests/test_sim_account.py tests/test_sim_strategy.py tests/test_sim_offpeak.py -q | . | passed | 0 | 1380 ms |
| 全量回归 | run_all_tests.py | . | passed | 0 | 32499 ms |

## Blockers

_None._

## Risks and skipped work

- save 不做即时 normalize_params，归一化延迟到 get_adapter 实例化；因 GET 与巡检均经 get_adapter 返回归一化值，行为等价（Verifier 判定低风险）
- 迁移后的 level_scale 键保留在 strategy_params 中但 adapter 改用 scale_* 标量参数，属冗余数据（不违反 A2 无丢失），后续迁移可在配置清理由清理
- 并发评估下 _consec_source_fails 为实例共享变量，计数存在轻微竞态，但阈值 20 与提前终止语义不受影响（工程折中）

## Previous iterations

| Goal cycle | Iteration | Attempt | Outcome | Unresolved | Summary | Completed |
| ---: | ---: | ---: | --- | --- | --- | --- |
| 1 | 1 | 0 | recovery | — | Native confirmed acceptance criteria changed | 2026-09-01T13:00:12.880Z |
| 2 | 1 | 1 | pass | — | 独立验收：逐项读取 brief/spec/实现与测试。A1-A6 全部通过（部分保存、迁移幂等、schema 契约与回退、前端动态渲染、错峰推迟可见、限流提前终止）；A7-A57 覆盖三个 capability 的全部可执行语义。Constraints 未违反：账户内核不 import 策略层、Decision 契约字段未变、单轮互斥/原子写/损坏回退/样本不足披露纪律保持。Runtime 检查：pytest 3 文件 37/37、run_all_tests 51/51。3 项低风险已记录。 | 2026-09-01T13:12:59.105Z |

## Conclusion

独立验收：逐项读取 brief/spec/实现与测试。A1-A6 全部通过（部分保存、迁移幂等、schema 契约与回退、前端动态渲染、错峰推迟可见、限流提前终止）；A7-A57 覆盖三个 capability 的全部可执行语义。Constraints 未违反：账户内核不 import 策略层、Decision 契约字段未变、单轮互斥/原子写/损坏回退/样本不足披露纪律保持。Runtime 检查：pytest 3 文件 37/37、run_all_tests 51/51。3 项低风险已记录。
